import { AttestationNaissance } from './attestation-naissance';

describe('AttestationNaissance', () => {
  it('should create an instance', () => {
    expect(new AttestationNaissance()).toBeTruthy();
  });
});
